# Changelog

本项目的版本更新记录。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

当前版本：**[v2.0.0](#200---2026-08-16)**

## [2.0.0] - 2026-08-16

### 新增：输入法状态监测（v2 核心功能）

- **中/英、全/半角状态气泡**：切换中文/英文输入法时弹出「中文输入·全角 / 英文输入·半角」提示，支持微软拼音、搜狗、QQ、百度等主流中文输入法
- **实时读取通道**（主通道，ImTip/aardio 方式）：每 30ms 轮询前台窗口，`WM_IME_CONTROL` 取转换模式按位判定——`(conv&3) 且无 NOCONVERSION(0x100)` 为中文、`conv&8` 为全角；**opened 通道实测在切换瞬间会抖动归零，故仅以 conv 位判定**，从根源上修复「识别反了」（切换输入法后气泡提示相反状态）
- **强制重读机制**：前台窗口变化或输入法切换（TSF 事件）时强制重读真实状态，解决「同一窗口切换输入法后提示相反」
- **键盘钩子兜底**：`WH_KEYBOARD_LL` 捕获 Shift（中/英）、Ctrl+Space（中/英）、Shift+Space（全/半角）、Win+Space（语言）切换热键，读不到真实状态时按语义翻转即时弹气泡
  - 单独 Shift 需「按下 ≥160ms 且无其他键同按」才判定为中英切换，避免快速击键误判导致状态漂移
  - Win+Space 切换的是系统输入语言布局，不参与中英翻转（由 TSF 语言事件单独处理）
- **TSF 事件通知**（Win8+）：注册 `ITfActiveLanguageProfileNotifySink`，切语言/切输入法即时触发强制重读
- **IMM 兜底**（传统 Win32 应用）：`ImmGetConversionStatus` 双路径，兼容 Win7

### 新增：文本输入场景过滤（减少打扰）

- 被动触发的输入法提示**仅在前台窗口有输入光标（caret）时弹出**——解压软件、看图器、文件管理器等无文本输入需求的窗口切到时不弹输入法气泡，避免频繁打扰
- 主动按 Shift / Ctrl+Space / Shift+Space 切换**任何场景都反馈**（用户明确操作）

### 新增：Win11 Fluent 风格 UI 重构

- **OSD 气泡**：白色渐变背景 + 大圆角（14px）+ 三层柔和阴影 + 主色蓝图标方块；开=深蓝文字、关=中性灰文字
- **三键专属色**：Caps=黄、Num=蓝、Scroll=红（与键盘指示灯惯例对应）；关状态统一中性灰并用白色 × 标记（Num/Scroll），避免斜划线字形重合；Caps 关用小写 a 区分
- **气泡宽度按内容自适应**：三键短文本气泡窄、输入法状态长文本气泡宽，不再统一取最大宽度
- **自绘右键菜单**：淡蓝 hover 圆角底 + 主色蓝勾选圆点 + 选中项加粗 + 更淡分隔线；仅保留 开机自启 / 显示时长 / 透明度 / 显示设置 / 退出
- **显示设置对话框**：浅色背景 + 圆角卡片 + 自绘蓝色主按钮；卡片标题大号加粗 + 右侧「实时预览」小字；选中单选/复选文字变主色蓝高亮
  - **点选即保存**：每次修改立即生效并写入配置（原「确定才保存/取消还原」改为即时保存，取消按钮改为「关闭」）

### 变更

- 默认气泡位置：屏幕正中 → **跟随鼠标**
- 默认透明度：75% → **100%**
- 显示时长默认 0.8s、启动提示 1.2s（保持）
- 托盘右键菜单精简为五项（移除了显示项多级菜单，统一进显示设置）
- 托盘图标：科技蓝底 → 纯色底；Caps 开=黄 A、关=灰 a

### 文档与工具

- 新增诊断工具：`test_ime_live.cpp`（实时输入法状态双通道诊断）、`test_ime_diag.cpp`、`test_indicator_*.cpp`、`test_tray_diag.cpp`
- README 重写为最新 UI 截图与演示视频

## [1.0.0] - 2026-07

### 新增

- **三键状态气泡**：按下 Caps Lock / Num Lock / Scroll Lock 时，屏幕下方居中弹出当前开关状态气泡（科技蓝圆角方块 + 白色加粗字符，A/a、N/n、⇅）
- **托盘图标常驻**：黄色 A=大写开、灰色 a=大写关，悬停提示同步
- **托盘右键菜单**：开机自启开关 / 显示项多级菜单 / 退出
- **开机自启**：管理员运行写 `HKLM`（所有用户）、普通权限写 `HKCU`（当前用户）
- **多显示器适配**：气泡定位基于鼠标所在屏幕底部居中（MonitorFromPoint + GetMonitorInfo）
- **显示动画**：启动 1.2s、切换 0.5s，淡出过渡
- **配置持久化**：同目录 `LockOnScreen.ini` + 注册表 `HKCU\Software\LockOnScreen` 兜底
- 32 位单文件绿色版（约 166 KB），仅依赖系统 DLL，Windows 7/10/11 可运行

### 修复

- 去掉半透明白高光描边与投影（避免左下/右下角出现白边模糊），改为深色细描边

---

## 待验证 / 后续计划

- Win7 实机验证（IMM 兜底通道）
- 搜狗/QQ/百度等第三方输入法兼容性精测
- 已决定不做：数字签名（SmartScreen「未知发布者」提示接受现状）

[2.0.0]: https://github.com/mrdalin/LockOnScreen/releases/tag/v2.0.0
[1.0.0]: https://github.com/mrdalin/LockOnScreen/releases/tag/v1.0.0